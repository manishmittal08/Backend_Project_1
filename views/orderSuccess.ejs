<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Order Successfully</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 min-h-screen flex items-center justify-center text-white">
  <div class="bg-zinc-800 p-8 rounded-lg w-full max-w-xl">
       <h1 class="text-3xl font-bold text-green-500 mb-4 text-center">🎉 Order Placed Successfully</h1>
       <p class="text-center mb-2">Order ID: <span class="text-blue-400 font-semibold"><%= order._id %>
         </span>
       </p>
       <p class="text-center mb-4">Confirmation email sent to:
          <span class="text-blue-400"><%= order.user.email %></span>
        </p>
        <hr class="border-zinc-700 my-4">
     <% order.items.forEach(item => { %>
       <div class="flex justify-between py-2">
         <span>
          <%= item.product.title %> × <%= item.quantity %>
         </span>
         <span>₹<%= item.price * item.quantity %></span>
       </div>
     <% }) %>
       <hr class="border-zinc-700 my-4">
       <div class="space-y-2 mb-4">
         <div class="flex justify-between text-sm">
           <span>Subtotal:</span>
           <span>₹<%= order.grandTotal %></span>
         </div>
         <% if(order.discountamount > 0) { %>
           <div class="flex justify-between text-sm text-green-400">
             <span>Auto Discount:</span>
             <span>-₹<%= order.discountamount %></span>
           </div>
         <% } %>
         <% if(order.couponUsed) { %>
           <div class="flex justify-between text-sm text-green-400">
             <span>Coupon (<%= order.couponUsed %>):</span>
             <span>-₹<%= order.couponDiscount %></span>
           </div>
         <% } %>
       </div>
       <hr class="border-zinc-700 my-4">
       <div class="flex justify-between text-lg font-bold">
           <span>Total Paid</span>
           <span class="text-green-400">
              ₹<%= order.finalamount %>
            </span>
        </div>
       <div class="text-center mt-6">
         <a href="/products" class="inline-block bg-blue-600 px-6 py-2 rounded hover:bg-blue-700">
            Continue Shopping
        </a>
       </div>

       <hr class="border-white border-1 my-4">

       <div class="grid grid-cols-2 gap-4 text-center">
      <div>
        <p class="text-2xl mb-2">📞</p>
        <p class="text-xs text-gray-400 mb-1">Call Us</p>
        <p class="font-semibold text-blue-400">+91 9990009999</p>
      </div>
      <div>
        <p class="text-2xl mb-2">✉️</p>
        <p class="text-xs text-gray-400 mb-1">Email Us</p>
        <p class="font-semibold text-blue-400">manish@gmail.com</p>
      </div>
       </div>

  </div>

</body>
</html>
